%table.table
  %thead
    %tr
      %th value
      %th amount
      %th users
    %tbody
      - @vote_result.each do |vr|
        %tr
          %td= vr[0]
          %td= vr[1][:score]
          %td= vr[1][:users].join(', ')
